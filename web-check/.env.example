# web-check has no NAS paths, TZ, or user directives besides Traefik routing

# Traefik router subdomain (private tier)
WEB_CHECK_SUBDOMAIN=web-check

# Shared Traefik vars (repo-wide, same value across every service's stack)
DOMAIN=example.com
TRAEFIK_ENTRYPOINT_3=private
