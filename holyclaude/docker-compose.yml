services:
  holyclaude:
    image: coderluii/holyclaude:latest
    container_name: Holyclaude
    hostname: holyclaude
    restart: unless-stopped
    shm_size: 4g
    cap_add:
      - SYS_ADMIN
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined
    ports:
      - 3059:3001
    volumes:
      - /volume2/docker/claude/data:/home/claude/.claude:rw
      - /volume2/docker/claude/workspace:/workspace:rw
    environment:
      TZ: "Europe/Zurich"
      PUID: 1000
      PGID: 10
      # ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY}"
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - cloudflare_web_network

networks:
  cloudflare_web_network:
    name: cloudflare-web
    external: true
