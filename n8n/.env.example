# Timezone
TZ=America/New_York

# PostgreSQL database
POSTGRES_PASSWORD=your-db-password-here

# n8n configuration
N8N_HOST=n8n.example.com
N8N_ENCRYPTION_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
WEBHOOK_URL=https://n8n.example.com/

# Optional Discord webhook for notifications
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook-here

# Volume base paths
DOCKER_CONFIG_DIR=/opt/docker
DOCKER_DATA_DIR=/opt/docker-data

# Traefik private-tier router hostname segment (n8n.${DOMAIN})
N8N_SUBDOMAIN=n8n

# Shared Traefik vars (repo-wide, same value across every service's stack)
DOMAIN=example.com
TRAEFIK_ENTRYPOINT_3=private
POSTGRES_DB=n8n
POSTGRES_USER=n8n
