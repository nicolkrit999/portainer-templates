# Static IP for AdGuard on the LAN (macvlan)
ADGUARD_STATIC_IP=192.168.1.100

# Host network interface and subnet for macvlan
LAN_INTERFACE=eth0
LAN_SUBNET=192.168.1.0/24
LAN_GATEWAY=192.168.1.1

# Volume base paths
DOCKER_CONFIG_DIR=/opt/docker
