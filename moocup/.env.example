# Traefik private-tier router hostname segment (moocup.${DOMAIN})
MOOCUP_SUBDOMAIN=moocup

# Shared Traefik vars (repo-wide, same value across every service's stack)
DOMAIN=example.com
TRAEFIK_ENTRYPOINT_3=private
