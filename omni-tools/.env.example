# omni-tools has no NAS paths, TZ, or user directives — no env vars required

# Traefik reverse-proxy hostname (private tier only; legacy Cloudflare override, no dash)
OMNI_TOOLS_SUBDOMAIN=omnitools

# Shared Traefik vars (repo-wide, same value across every service's stack)
DOMAIN=example.com
TRAEFIK_ENTRYPOINT_1=family
